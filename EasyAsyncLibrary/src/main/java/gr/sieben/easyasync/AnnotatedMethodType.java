package gr.sieben.easyasync;

/**
 * Enumerator that defines the parameter type of the annotated method that needs to be invoked
 */
enum AnnotatedMethodType {
    NO_PARAM, TYPE_CALLBACKS, TYPE_RESULT
}
